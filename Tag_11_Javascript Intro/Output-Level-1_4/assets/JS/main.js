window.alert("Hallo Welt");

// Popup Fenster mit Hello World

window.prompt("Bitte geben Sie Ihren Namen ein.");

// Popup Fenster mit Eingabefunktion (Name). Kann bestätigt oder abgelehnt werden.

window.prompt("Bitte geben Sie Ihren Namen ein", "Cancel");

// Popup Fenster mit Eingabefunktion (Name). Kann bestätigt oder abgelehnt werden. Zusätzlicher Platzhalter Text im Fenster.

window.confirm("Stimmen Sie meiner Meinung zu?");

// Popup Fenster zur Bestätigung. Kann bestätigt oder abgelehnt werden.

let alter = window.prompt("Gib mir bitte dein Alter:");
console.log(alter);

// Popup Fenster zur Alterseingabe. Kann bestätigt oder abgelehnt werden.

let b = 5;
let a = b;

5 - 3;
window.alert(a);

//Popup Fenster mit Zahl

let mann = "Robert Wadlow ist der größte Mann der Welt : ";
let grosse = "2,72m";
window.confirm(mann + grosse);

// Popup Fenster mit zusammengefügten Strings
